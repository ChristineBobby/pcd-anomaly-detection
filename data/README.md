# Data Directory

Place local datasets, checkpoints, and PASDF preprocessed SDF samples here.

This directory is ignored by git except for this README. Record download links,
checksums, and directory conventions in `docs/` or experiment notes instead of
committing data files.
